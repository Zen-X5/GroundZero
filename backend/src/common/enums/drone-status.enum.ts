export enum DroneStatus {
  IDLE = 'idle',
  LAUNCHING = 'launching',
  SEARCHING = 'searching',
  INSPECTING = 'inspecting',
  RETURNING = 'returning',
  CHARGING = 'charging',
  DISCONNECTED = 'disconnected',
  FAILED = 'failed',
}
